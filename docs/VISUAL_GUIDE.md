# Visual Guide - Diagram Alur Kerja

Dokumen ini berisi diagram-diagram visual untuk memahami alur kerja sistem CRUD.

## 🏗️ Arsitektur MVC (Model-View-Controller)

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER (Browser)                          │
│                    http://localhost:8000                        │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ 1. Request (HTTP GET/POST/PUT/DELETE)
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                          ROUTES                                 │
│                      routes/web.php                             │
│                                                                   │
│  Route::resource('products', ProductController::class);         │
│                                                                   │
│  Mapping:                                                        │
│  GET    /products          → index()     (List semua)           │
│  GET    /products/create   → create()    (Form create)          │
│  POST   /products          → store()     (Simpan baru)          │
│  GET    /products/{id}     → show()      (Detail satu)          │
│  GET    /products/{id}/edit → edit()     (Form edit)            │
│  PUT    /products/{id}     → update()    (Update data)          │
│  DELETE /products/{id}     → destroy()   (Hapus data)           │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ 2. Route ke Controller
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                       CONTROLLER                                │
│             app/Http/Controllers/ProductController.php          │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ public function index()                                 │   │
│  │ {                                                       │   │
│  │     // 3. Query ke Model                               │   │
│  │     $products = Product::paginate(10);                 │   │
│  │                                                        │   │
│  │     // 5. Render View                                  │   │
│  │     return view('products.index', compact('products'));│  │
│  │ }                                                       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                   │
│  Tugas Controller:                                               │
│  ✓ Handle request dari user                                      │
│  ✓ Validasi input                                                │
│  ✓ Panggil Model untuk database operations                       │
│  ✓ Return View ke user                                           │
└──────────────┬────────────────────────────┬───────────────────────┘
               │                            │
               │ 3. Query Database          │ 5. Return Data
               ▼                            ▼
┌──────────────────────────┐    ┌──────────────────────────────────┐
│         MODEL            │    │           VIEW                    │
│   app/Models/Product.php  │    │ resources/views/products/        │
│                          │    │                                  │
│  class Product extends    │    │  ┌────────────────────────────┐ │
│   Model                   │    │  │ @extends('layouts.app')    │ │
│  {                        │    │  │ @section('title', 'Products')││
│    protected $fillable =  │    │  │ @section('content')         │ │
│    ['name', 'price', ...];│    │  │  <table>                   │ │
│  }                        │    │  │   @foreach($products as $p)│ │
│                          │    │  │   <tr>                     │ │
│  Tugas Model:             │    │  │     <td>{{ $p->name }}</td>│ │
│  ✓ Represent tabel DB     │    │  │     <td>{{ $p->price }}</td>││
│  ✓ Query database         │    │  │   @endforeach             │ │
│  ✓ Relationship logic     │    │  │  </table>                  │ │
│                          │    │  │  @endforeach               │ │
└──────────┬───────────────┘    │  │ @endsection                │ │
           │ 4. Return Data     │  └────────────────────────────┘ │
           │                    │                                  │
           └────────────────────┴──────────────────────────────────┘
                                        │
                                        │ 6. HTML Response
                                        ▼
                              ┌─────────────────────────┐
                              │   Browser Displays      │
                              │   HTML Page to User     │
                              └─────────────────────────┘
```

---

## 📊 Alur Lengkap: Create Product

```
USER                    BROWSER                ROUTES              CONTROLLER
  │                       │                       │                    │
  │ 1. Click "Add"        │                       │                    │
  │──────────────────────>│                       │                    │
  │                       │ 2. GET /products/create│                    │
  │                       │───────────────────────>│                    │
  │                       │                       │ 3. Route to        │
  │                       │                       │   create()         │
  │                       │                       │────────────────────>│
  │                       │                       │                    │
  │                       │ 5. Return HTML form   │ 4. Return view    │
  │<──────────────────────│<──────────────────────│<────────────────────│
  │                       │                       │                    │
  │ 6. Fill form          │                       │                    │
  │    Name: "Laptop"     │                       │                    │
  │    Price: 15000000    │                       │                    │
  │    Quantity: 50       │                       │                    │
  │                       │                       │                    │
  │ 7. Click "Submit"     │                       │                    │
  │──────────────────────>│                       │                    │
  │                       │ 8. POST /products     │                    │
  │                       │    (with form data)   │                    │
  │                       │───────────────────────>│                    │
  │                       │                       │ 9. Route to store()│
  │                       │                       │────────────────────>│
  │                       │                       │                    │
  │                       │                       │ 10. Validate       │
  │                       │                       │     Request        │
  │                       │                       │                    │
  │                       │                       │ 11. Call Model     │
  │                       │                       │─────────────────┐  │
  │                       │                       │                 │  │
  │                       │                       │                 ▼  ▼
  │                       │                       │              MODEL (Product)
  │                       │                       │              Product::create([
  │                       │                       │                'name' => 'Laptop',
  │                       │                       │                'price' => 15000000,
  │                       │                       │                'quantity' => 50
  │                       │                       │              ])
  │                       │                       │                    │
  │                       │                       │ 12. Insert to DB    │
  │                       │                       │─────────────────┐  │
  │                       │                       │                 │  │
  │                       │                       │                 ▼  ▼
  │                       │                       │              DATABASE
  │                       │                       │              INSERT INTO products
  │                       │                       │              (name, price, quantity)
  │                       │                       │              VALUES ('Laptop', 15000000, 50)
  │                       │                       │                    │
  │                       │                       │ 13. Return success  │
  │                       │<──────────────────────│<────────────────────│
  │                       │    (Redirect to list) │                    │
  │<──────────────────────│                       │                    │
  │ 14. See updated list  │                       │                    │
  │    with new product   │                       │                    │
```

---

## 🔄 Alur Update Product

```
USER                  BROWSER              ROUTES            CONTROLLER
 │                      │                    │                   │
 │ 1. Click "Edit"      │                    │                   │
 │─────────────────────>│                    │                   │
 │                      │ 2. GET /products/3/edit               │
 │                      │────────────────────>│                   │
 │                      │                    │ 3. Route to edit()│
 │                      │                    │───────────────────>│
 │                      │                    │                   │
 │                      │ 5. Return edit form│ 4. Find product   │
 │                      │    with data       │    $product = Product::find(3)
 │<─────────────────────│<────────────────────│<───────────────────│
 │                      │                    │                   │
 │ 6. Modify data       │                    │                   │
 │    Price: 14000000   │                    │                   │
 │                      │                    │                   │
 │ 7. Click "Update"    │                    │                   │
 │─────────────────────>│                    │                   │
 │                      │ 8. PUT /products/3 │                   │
 │                      │────────────────────>│ 9. Route to update()
 │                      │                    │───────────────────>│
 │                      │                    │                   │
 │                      │                    │ 10. Validate      │
 │                      │                    │ 11. Update model  │
 │                      │                    │─────────────────┐ │
 │                      │                    │                 │ │
 │                      │                    │                 ▼ ▼
 │                      │                    │              PRODUCT MODEL
 │                      │                    │              $product->update([
 │                      │                    │                'price' => 14000000
 │                      │                    │              ])
 │                      │                    │                   │
 │                      │                    │ 12. UPDATE products│
 │                      │                    │─────────────────┐ │
 │                      │                    │                 │ │
 │                      │                    │                 ▼ ▼
 │                      │                    │              DATABASE
 │                      │                    │              UPDATE products
 │                      │                    │              SET price = 14000000
 │                      │                    │              WHERE id = 3
 │                      │                    │                   │
 │                      │ 13. Redirect        │                   │
 │<─────────────────────│<────────────────────│<───────────────────│
 │                      │                    │                   │
 │ 14. See updated data │                    │                   │
```

---

## 🗑️ Alur Delete Product

```
USER                BROWSER           ROUTES          CONTROLLER
 │                   │                  │                │
 │ 1. Click "Delete" │                  │                │
 │──────────────────>│                  │                │
 │                   │ 2. Confirm       │                │
 │    OK             │                  │                │
 │                   │                  │                │
 │                   │ 3. DELETE /products/5             │
 │                   │─────────────────>│                │
 │                   │                  │ 4. Route to destroy()
 │                   │                  │─────────────────>│
 │                   │                  │                │
 │                   │                  │ 5. Find product │
 │                   │                  │    $product = Product::find(5)
 │                   │                  │                │
 │                   │                  │ 6. Delete       │
 │                   │                  │─────────────────┐│
 │                   │                  │                 ││
 │                   │                  │                 ▼▼
 │                   │                  │              PRODUCT MODEL
 │                   │                  │              $product->delete()
 │                   │                  │                │
 │                   │                  │ 7. DELETE FROM products│
 │                   │                  │─────────────────┐│
 │                   │                  │                 ││
 │                   │                  │                 ▼▼
 │                   │                  │              DATABASE
 │                   │                  │              DELETE FROM products
 │                   │                  │              WHERE id = 5
 │                   │                  │                │
 │                   │ 8. Redirect      │                │
 │<──────────────────│<─────────────────│<────────────────│
 │                   │                  │                │
 │ 9. See list       │                  │                │
 │    (product deleted)                 │                │
```

---

## 🗄️ Struktur Database

```
┌─────────────────────────────────────────────────────────────┐
│                    CRUD LARAVEL DATABASE                    │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  PRODUCTS                                            │  │
│  ├──────────────────────────────────────────────────────┤  │
│  │  id          INT (PK, Auto Increment)                │  │
│  │  name        VARCHAR(255)                            │  │
│  │  description TEXT                                    │  │
│  │  price       DECIMAL(10,2)                           │  │
│  │  quantity    INT                                     │  │
│  │  created_at  TIMESTAMP                               │  │
│  │  updated_at  TIMESTAMP                               │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  CATEGORIES                                          │  │
│  ├──────────────────────────────────────────────────────┤  │
│  │  id          INT (PK, Auto Increment)                │  │
│  │  name        VARCHAR(255)                            │  │
│  │  description TEXT                                    │  │
│  │  slug        VARCHAR(255) (Unique)                   │  │
│  │  is_active   BOOLEAN (Default: true)                 │  │
│  │  created_at  TIMESTAMP                               │  │
│  │  updated_at  TIMESTAMP                               │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  ORDERS                                              │  │
│  ├──────────────────────────────────────────────────────┤  │
│  │  id              INT (PK, Auto Increment)            │  │
│  │  order_number   VARCHAR(50) (Unique)                 │  │
│  │  customer_name  VARCHAR(255)                         │  │
│  │  customer_email VARCHAR(255)                         │  │
│  │  customer_phone VARCHAR(20)                          │  │
│  │  total_amount   DECIMAL(10,2)                        │  │
│  │  status         ENUM('pending','processing',         │  │
│  │                 'completed','cancelled')             │  │
│  │  notes          TEXT (Nullable)                      │  │
│  │  created_at     TIMESTAMP                            │  │
│  │  updated_at     TIMESTAMP                            │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  USERS                                               │  │
│  ├──────────────────────────────────────────────────────┤  │
│  │  id          INT (PK, Auto Increment)                │  │
│  │  name        VARCHAR(255)                            │  │
│  │  email       VARCHAR(255) (Unique)                   │  │
│  │  password    VARCHAR(255) (Hashed)                   │  │
│  │  status      ENUM('active','inactive',               │  │
│  │              'suspended','pending')                  │  │
│  │  role        VARCHAR(20) (Default: 'user')           │  │
│  │  created_at  TIMESTAMP                               │  │
│  │  updated_at  TIMESTAMP                               │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎨 Struktur Views (Blade Templates)

```
resources/views/
│
├── admin/
│   ├── layouts/
│   │   └── app.blade.php            ← MASTER LAYOUT (Sidebar, Header, Footer)
│   │                                 Berisi: @yield('content'), @yield('title')
│   │
│   ├── dashboard.blade.php           ← DASHBOARD HOME
│   │   @extends('admin.layouts.app')
│   │   @section('title', 'Dashboard')
│   │   @section('content') ... statistik cards ...
│   │
│   ├── products/
│   │   ├── index.blade.php          ← LIST PRODUCTS
│   │   │   @extends('admin.layouts.app')
│   │   │   @section('content')
│   │   │       Table dengan @foreach($products as $product)
│   │   │       Tombol: Add, Edit, View, Delete
│   │   │       Pagination: {{ $products->links() }}
│   │   │
│   │   ├── create.blade.php         ← FORM CREATE
│   │   │   @extends('admin.layouts.app')
│   │   │   @section('content')
│   │   │       <form method="POST" action="{{ route('products.store') }}">
│   │   │           @csrf
│   │   │           Input: name, description, price, quantity
│   │   │           <button type="submit">Submit</button>
│   │   │       </form>
│   │   │
│   │   ├── edit.blade.php           ← FORM EDIT
│   │   │   @extends('admin.layouts.app')
│   │   │   @section('content')
│   │   │       <form method="POST" action="{{ route('products.update', $product) }}">
│   │   │           @csrf
│   │   │           @method('PUT')
│   │   │           Input: (sama seperti create, tapi ada value="{{ $product->name }}")
│   │   │           <button type="submit">Update</button>
│   │   │       </form>
│   │   │
│   │   └── show.blade.php           ← DETAIL PRODUCT
│   │       @extends('admin.layouts.app')
│   │       @section('content')
│   │           Table detail: ID, Name, Description, Price, Quantity
│   │           Tombol: Back, Edit, Delete
│   │
│   ├── categories/                  ← SAMA STRUCTURE DENGAN PRODUCTS
│   │   ├── index.blade.php
│   │   ├── create.blade.php
│   │   ├── edit.blade.php
│   │   └── show.blade.php
│   │
│   ├── orders/                      ← SAMA STRUCTURE DENGAN PRODUCTS
│   │   ├── index.blade.php
│   │   ├── create.blade.php
│   │   ├── edit.blade.php
│   │   └── show.blade.php
│   │
│   └── users/                       ← SAMA STRUCTURE DENGAN PRODUCTS
│       ├── index.blade.php
│       ├── create.blade.php
│       ├── edit.blade.php
│       └── show.blade.php
```

---

## 🔐 Flow Validation

```
USER SUBMIT FORM
       │
       ▼
CONTROLLER VALIDATES
       │
       ├─→ VALIDATION FAILED?
       │   │
       │   └─→ YES: Redirect back with errors
       │       │
       │       ▼
       │   VIEW SHOWS ERRORS
       │       │
       │       └─→ User sees: "The name field is required."
       │           User corrects form
       │           User resubmits
       │
       └─→ VALIDATION PASSED?
           │
           └─→ YES: Continue to business logic
               │
               ├─→ Model::create() or $model->update()
               │
               └─→ Redirect with success message
                   │
                   ▼
              VIEW SHOWS SUCCESS
                  │
                  └─→ User sees: "Product created successfully!"
                      User redirected to list
```

---

## 📱 Layout Structure

```
┌──────────────────────────────────────────────────────────────┐
│  <html>                                                      │
│  <head>                                                      │
│    - CSS (Bootstrap 5.3.2)                                   │
│    - Bootstrap Icons                                        │
│    - Custom Styles                                          │
│  </head>                                                     │
│                                                              │
│  <body>                                                      │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  NAVBAR (Top)                                          │ │
│  │  - Logo                                                │ │
│  │  - Toggle (Mobile)                                     │ │
│  │  - User profile                                        │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  ┌─────────┬──────────────────────────────────────────────┐ │
│  │         │                                              │ │
│  │ SIDEBAR │               CONTENT AREA                   │ │
│  │ (Left)  │               (@yield('content'))            │ │
│  │         │                                              │ │
│  │ - 📊    │  ┌────────────────────────────────────────┐ │ │
│  │   Dashboard│                                         │ │ │
│  │          │  │                                        │ │ │
│  │ - 📦     │  │         DYNAMIC CONTENT                │ │ │
│  │   Products│  │         (Changes per page)            │ │ │
│  │          │  │                                        │ │ │
│  │ - 🏷️     │  │         - List/Table                  │ │ │
│  │   Categories│        - Form                         │ │ │
│  │          │  │         - Detail                      │ │ │
│  │ - 🛒     │  │         - Statistics                  │ │ │
│  │   Orders │  │                                        │ │ │
│  │          │  │                                        │ │ │
│  │ - 👤     │  │                                        │ │ │
│  │   Users  │  │                                        │ │ │
│  │          │  │                                        │ │ │
│  │          │  └────────────────────────────────────────┘ │ │
│  └─────────┴──────────────────────────────────────────────┘ │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  FOOTER                                                 │ │
│  │  - Copyright                                            │ │
│  │  - Scripts (jQuery, Bootstrap JS)                       │ │
│  └────────────────────────────────────────────────────────┘ │
│  </body>                                                     │
│  </html>                                                     │
└──────────────────────────────────────────────────────────────┘
```

---

## 🔄 Request Lifecycle (Laravel)

```
1. USER REQUEST
   │
   ├─→ http://localhost:8000/products
   │
   ▼
2. PUBLIC/index.php (Entry Point)
   │
   ├─→ Load Bootstrap
   ├─→ Create Kernel
   ├─→ Handle Request
   │
   ▼
3. MIDDLEWARE (Layers)
   │
   ├─→ [EncryptCookies]
   ├─→ [AddQueuedCookiesToResponse]
   ├─→ [StartSession]
   ├─→ [Authenticate] (Check if user logged in)
   ├─→ [VerifyCsrfToken] (Security check)
   ├─→ [ShareErrorsFromSession]
   │
   ▼
4. ROUTES (routes/web.php)
   │
   ├─→ Route::resource('products', ProductController::class)
   ├─→ Match: GET /products
   ├─→ Target: ProductController@index
   │
   ▼
5. CONTROLLER (app/Http/Controllers/ProductController.php)
   │
   ├─→ public function index()
   ├─→ {
   ├─→     $products = Product::paginate(10);
   ├─→     return view('products.index', compact('products'));
   ├─→ }
   │
   ▼
6. MODEL (app/Models/Product.php)
   │
   ├─→ Product::paginate(10)
   ├─→ Query Builder ke Database
   │
   ▼
7. DATABASE (SQLite)
   │
   ├─→ Execute: SELECT * FROM products LIMIT 10 OFFSET 0
   ├─→ Return: Collection of Product objects
   │
   ▼
8. VIEW (resources/views/products/index.blade.php)
   │
   ├─→ @extends('layouts.app')
   ├─→ @section('content')
   ├─→     Render HTML dengan data products
   ├─→ @endsection
   │
   ▼
9. RESPONSE
   │
   ├─→ HTML Page generated
   ├─→ Send to Browser
   │
   ▼
10. BROWSER DISPLAYS
    │
    └─→ User sees Product List
```

---

## 🔍 Search & Filter Flow

```
USER TYPE SEARCH QUERY
       │
       │ "Laptop"
       ▼
USER CLICK SEARCH/ENTER
       │
       ▼
FORM SUBMIT: GET /products?search=Laptop
       │
       ▼
CONTROLLER index(Request $request)
       │
       ├─→ Check if has search parameter
       │
       ├─→ if ($request->has('search'))
       │   {
       │       $query->where('name', 'like', '%' . $request->search . '%');
       │   }
       │
       └─→ Execute SQL:
           SELECT * FROM products
           WHERE name LIKE '%Laptop%'
           ORDER BY created_at DESC
           LIMIT 10
       │
       ▼
RETURN FILTERED RESULTS
       │
       ▼
VIEW DISPLAYS FILTERED LIST
       │
       └─→ User sees products matching "Laptop"
```

---

## 📊 Pagination Flow

```
DATABASE: 100 Products
         │
         ▼
PAGINATION: 10 per page
         │
         ├─→ Page 1: Products 1-10
         ├─→ Page 2: Products 11-20
         ├─→ Page 3: Products 21-30
         ├─→ ...
         └─→ Page 10: Products 91-100
         │
         ▼
USER VIEW
         │
         ├─→ Table shows 10 products
         ├─→ Pagination links: [1] [2] [3] ... [10]
         │   Next
         │
         ▼
USER CLICK PAGE 2
         │
         ▼
REQUEST: GET /products?page=2
         │
         ▼
QUERY: SELECT * FROM products
        LIMIT 10 OFFSET 10
        │
        ▼
DISPLAY: Products 11-20
        │
        └─→ Pagination: 1 [2] 3 ... 10
            Prev Next
```

---

## 🎯 Key Concepts Visual Summary

### **MODEL** = Database Interface
```
Database Table "products"
    ↑
    │ Represented by
    │
Product Model (Class)
    ↓
Used in Controller
```

### **CONTROLLER** = Business Logic
```
User Request → Controller → Model (query DB)
                        → View (render HTML)
                        → Response
```

### **VIEW** = Presentation Layer
```
Controller passes data → View → Blade renders HTML → Browser displays
```

### **ROUTE** = URL Mapping
```
URL: /products → Route → Controller method: index()
URL: /products/create → Route → Controller method: create()
URL: /products/3 → Route → Controller method: show(3)
```

---

**💡 Tip: Print diagram-diagram ini dan tempel di dinding saat belajar!**
